# ============================================
# Telegram服务器环境变量配置模板
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env：cp env.example .env
# 2. 修改 .env 中的配置为实际值
# 3. 不要将 .env 提交到 Git！
#
# 安全建议：
# - 使用强密码（至少12位，包含大小写字母、数字、特殊字符）
# - 生产环境使用 DB_SSLMODE=require
# - 定期轮换密码和密钥
# - 参考 ENV_SECURITY.md 了解更多安全措施
#
# ============================================

# 数据库配置
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres  # ⚠️ 请修改为强密码
DB_NAME=youdu_db
DB_SSLMODE=disable  # 生产环境建议改为 require

# 服务器配置
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# WebSocket配置
WS_PORT=8081
WS_HOST=0.0.0.0

# JWT密钥（用于生成和验证登录令牌）
JWT_SECRET=your_jwt_secret_key_at_least_32_characters  # ⚠️ 请使用至少32位随机字符

# 验证码过期时间（分钟）
VERIFY_CODE_EXPIRE_MINUTES=5

# 应用环境
APP_ENV=development

# 阿里云OSS配置（图片上传必需）
# 注意：请妥善保管 OSS 密钥，不要泄露
S3_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com  # 根据实际区域修改
S3_ACCESS_KEY=your-access-key-id  # ⚠️ 阿里云 AccessKey ID
S3_SECRET_KEY=your-secret-access-key  # ⚠️ 阿里云 AccessKey Secret
S3_BUCKET=your-bucket-name  # OSS Bucket 名称
S3_CDN_DOMAIN=  # CDN加速域名（可选，如：cdn.example.com）

# 测试环境OSS配置（可选，APP_ENV=debug/development时使用）
TEST_S3_ENDPOINT=
TEST_S3_ACCESS_KEY=
TEST_S3_SECRET_KEY=
TEST_S3_BUCKET=
TEST_S3_CDN_DOMAIN=  # 测试环境CDN加速域名（可选）

# Agora 音视频通话配置
# 获取方式：访问 https://console.agora.io/ 创建项目
AGORA_APP_ID=your-agora-app-id  # ⚠️ Agora App ID
AGORA_APP_CERTIFICATE=your-agora-app-certificate  # ⚠️ Agora App Certificate（用于生成Token）

# ============================================
# 配置完成后的操作：
# ============================================
# 
# 选项1：直接使用（开发环境）
#   - 确保 .env 不会被提交到 Git
#
# 选项2：加密保护（推荐）
#   cd tools
#   go run encrypt_env.go -encrypt -password="your-secure-password"
#   - 这会生成 .env.encrypted 文件
#   - .env.encrypted 可以安全地提交到 Git
#
# 选项3：使用系统环境变量（生产环境推荐）
#   - 不使用 .env 文件
#   - 直接设置系统环境变量
#
# 详细说明请参考 ENV_SECURITY.md
#

